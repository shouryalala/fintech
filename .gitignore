# Secrets — real MCP config holds a live API key, never commit it
.mcp.json

# Node
node_modules/
npm-debug.log*

# OS
.DS_Store

# Playwright MCP — runtime artifacts (console logs, page snapshots), not source
.playwright-mcp/
